(()=>{
  const $=id=>document.getElementById(id);
  const money=n=>Number(n||0).toFixed(2);
  let timer=null,lastRoundId='',chartRoot=null;
  const price=(r,p)=>{const b=105000+Number(r.roundNo)*1000,d=r.direction==='UP'?1:-1;const wave=Math.sin(p*18+Number(r.roundNo)*1.7)*.00075+Math.sin(p*47)*.00025;return b*(1+d*(.0004+.0044*p)+wave)};
  function state(r){const n=Date.now()/1000,s=Number(r.startAt),e=s+Number(r.durationSeconds),w=s+Number(r.entryWindowSeconds||Math.max(15,Math.floor(Number(r.durationSeconds)*.2)));return n<s?'UPCOMING':n<w?'ENTRY_OPEN':n<e?'LOCKED':'RESULT'}
  function makeChart(r){
    const total=56,b=105000+Number(r.roundNo)*1000,vals=[];
    for(let i=0;i<total;i++){const p=i/(total-1),c=price(r,p),o=i?price(r,Math.max(0,(i-1)/(total-1))):c;const open=o+(c-o)*.32,w=Math.abs(Math.sin(i*2.17+Number(r.roundNo)))*28+12;vals.push({open,close:c,high:Math.max(open,c)+w,low:Math.min(open,c)-w,p})}
    const all=vals.flatMap(c=>[c.high,c.low]),entry=price(r,0),exit=price(r,1),mn=Math.min(...all,entry,exit)-45,mx=Math.max(...all,entry,exit)+45,y=v=>292-(v-mn)/(mx-mn)*255,step=840/(total-1);
    let s='<svg id="rh-live-svg" viewBox="0 0 900 330" preserveAspectRatio="none"><rect width="900" height="330" fill="#070b12"/>';
    for(let i=0;i<7;i++)s+=`<line x1="0" y1="${32+i*43}" x2="900" y2="${32+i*43}" stroke="#ffffff0c"/>`;
    vals.forEach((c,i)=>{const x=30+i*step,w=Math.min(12,Math.max(6,step*.42)),up=c.close>=c.open,bodyY=Math.min(y(c.open),y(c.close)),bodyH=Math.max(3,Math.abs(y(c.close)-y(c.open)));s+=`<g class="rh-live-candle" data-i="${i}" opacity="0"><line x1="${x}" y1="${y(c.high)}" x2="${x}" y2="${y(c.low)}" stroke="${up?'#29c98a':'#ff5f70'}" stroke-width="2"/><rect x="${x-w/2}" y="${bodyY}" width="${w}" height="${bodyH}" rx="2" fill="${up?'#29c98a':'#ff5f70'}"/></g>`});
    s+=`<line x1="0" y1="${y(entry)}" x2="900" y2="${y(entry)}" stroke="#7c6cff" stroke-width="2" stroke-dasharray="7 6"/><text x="12" y="${y(entry)-7}" fill="#a99cff" font-size="12">ENTRY ${entry.toFixed(0)}</text><line id="rh-live-trade-line" x1="0" y1="${y(entry)}" x2="900" y2="${y(entry)}" stroke="#ffffff" stroke-width="2" opacity=".75"/><circle id="rh-live-price-dot" cx="30" cy="${y(entry)}" r="5" fill="#fff" stroke="#7c6cff" stroke-width="3"/><text id="rh-live-price-label" x="42" y="${y(entry)-9}" fill="#fff" font-size="12">${entry.toFixed(0)}</text><g id="rh-live-exit" opacity="0"><line x1="0" y1="${y(exit)}" x2="900" y2="${y(exit)}" stroke="#ffb84d" stroke-width="2" stroke-dasharray="7 6"/><text x="12" y="${y(exit)-7}" fill="#ffcf75" font-size="12">EXIT ${exit.toFixed(0)}</text></g></svg>`;
    return {html:s,y,vals,total};
  }
  function ensureChart(r){const el=$('rh-chart');if(!el)return null;if(chartRoot&&chartRoot.el===el&&chartRoot.id===r.id)return chartRoot;const c=makeChart(r);el.innerHTML=c.html;chartRoot={el,id:r.id,...c};return chartRoot}
  function updateChart(r){const c=ensureChart(r);if(!c)return;const now=Date.now()/1000,st=Number(r.startAt),du=Number(r.durationSeconds),p=Math.max(0,Math.min(1,(now-st)/du)),cur=price(r,p),idx=Math.min(c.total-1,Math.floor(p*(c.total-1)));c.el.querySelectorAll('.rh-live-candle').forEach((g,i)=>g.setAttribute('opacity',i<=idx?'1':'0'));const yy=c.y(cur),line=c.el.querySelector('#rh-live-trade-line'),dot=c.el.querySelector('#rh-live-price-dot'),lab=c.el.querySelector('#rh-live-price-label');if(line){line.setAttribute('y1',yy);line.setAttribute('y2',yy)}if(dot)dot.setAttribute('cy',yy);if(lab){lab.setAttribute('y',yy-9);lab.textContent=cur.toFixed(0)}const exit=c.el.querySelector('#rh-live-exit');if(exit)exit.setAttribute('opacity',p>=.995||state(r)==='RESULT'?'1':'0')}
  async function refresh(){const body=$('rh-body');if(!body)return;const label=body.querySelector('.rh-label')?.textContent||'',m=label.match(/ROUND\s+(\d+)/);if(!m)return;try{const x=await fetch('/api/rounds',{credentials:'same-origin',cache:'no-store'}),d=await x.json();if(!x.ok||!d.ok)return;const r=(d.rounds||[]).find(q=>String(q.roundNo)===m[1]);if(!r)return;if(lastRoundId!==r.id){lastRoundId=r.id;chartRoot=null}if(r.trade&&r.trade.status==='SETTLED')return;updateChart(r)}catch{}}
  function patchResults(){const rg=$('rg');if(!rg)return;rg.querySelectorAll('.rc').forEach(card=>{const b=card.querySelector('button');if(!b)return;const txt=(b.textContent||'').trim();if(txt!=='View Result'&&!card.textContent.includes('RESULT'))return;let id=(b.getAttribute('onclick')||'').match(/openRound\(['"]([^'"]+)/)?.[1];if(!id){const idx=[...document.querySelectorAll('.rc')].indexOf(card);if(idx>=0)id=window.__tradehubRounds?.[idx]?.id}if(id){b.disabled=false;b.textContent='View Result';b.className='bp';b.onclick=()=>window.openRound(id)}})}
  new MutationObserver(()=>{patchResults();if($('rh-chart'))refresh()}).observe(document.body,{childList:true,subtree:true});
  clearInterval(timer);timer=setInterval(refresh,1000);patchResults();
})();
