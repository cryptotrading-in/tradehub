(()=>{function patch(){const rg=document.getElementById('rg');if(!rg)return;rg.querySelectorAll('.rc').forEach(card=>{const b=card.querySelector('button');if(!b)return;const txt=(card.querySelector('.rs')?.textContent||card.textContent||'').trim();const result=txt.includes('RESULT');if(!result)return;const id=(b.getAttribute('onclick')||'').match(/openRound\(['"]([^'"]+)/)?.[1];if(!id)return;b.disabled=false;b.className='bp';b.textContent='View Result';b.onclick=()=>window.openRound(id)})}patch();const obs=new MutationObserver(patch);const rg=document.getElementById('rg');if(rg)obs.observe(rg,{childList:true,subtree:true});setInterval(patch,500)} )();