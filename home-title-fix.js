(()=>{let welcome='';function fix(){if(location.pathname!=='/'&&location.pathname!=='/index.html')return;const content=document.querySelector('.shell-content');if(content){const heroes=content.querySelectorAll('.shell-hero');for(let i=1;i<heroes.length;i++)heroes[i].remove()}const el=document.getElementById('title');if(!el)return;const text=el.textContent||'';if(text.startsWith('Welcome, ')){welcome=text;return}if(text==='Your trading experience starts here.')el.textContent=welcome}const start=()=>{fix();const root=document.querySelector('.shell-content')||document.body;if(root)new MutationObserver(fix).observe(root,{childList:true,characterData:true,subtree:true})};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start()})();