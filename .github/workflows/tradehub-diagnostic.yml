name: Tradehub Production Diagnostic

on:
  push:
    branches:
      - fix/home-controller-cleanup
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  diagnostic:
    runs-on: ubuntu-latest
    steps:
      - name: Runner smoke test
        run: |
          echo "TRADEHUB DIAGNOSTIC RUNNER: PASS"
          echo "Commit: $GITHUB_SHA"
